# Foil runs on-device by default and needs no configuration.
#
# This key only enables the CLOUD FALLBACK (Claude), used on devices that
# cannot run a local model (no WebGPU). The on-device tier never uses it.
ANTHROPIC_API_KEY=
