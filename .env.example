# Foil runs on Claude (cloud) by default, so this key is required for the
# default coach. Set it to the value of an Anthropic API key.
#
# The opt-in on-device tier (a small in-browser model) never uses this key;
# it is also the cloud fallback when a device cannot run a local model.
ANTHROPIC_API_KEY=
